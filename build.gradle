
apply plugin: 'java'

version = '1.0.0'
group = 'org.mike'

repositories {
	mavenCentral();
}

dependencies {
	testCompile 'junit:junit:4.12'
}

task wrapper(type: Wrapper) {
//	gradleVersion = '2.0'
}

jar {
	manifest {
		attributes("Implementation-Title": "Gradle",
					"Implementation-Version": version)
	}
}

uploadArchives {
	repositories {
		flatDir {
			dirs 'C:/Users/broadbear/local_repo'
		}
	}
}